// constants
import { grammarTypes } from "../../constants/GRAMMAR";

// styles
import classes from "./styles.module.css";

export default function TenseItem({ data }) {
  return <div className={classes.root}>(TenseItem)</div>;
}
